<UserControl x:Class="EscapeRoom.Views.ReservationHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:EscapeRoom.ViewModels"
             mc:Ignorable="d">
    <UserControl.DataContext>
        <vm:ReservationHistoryViewModel />
    </UserControl.DataContext>

    <Grid Background="#252525">
        <TextBlock Text="Twoje rezerwacje" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" Foreground="White"/>
        <ListView ItemsSource="{Binding UserReservations}" Margin="0,30,0,0" Foreground="Black">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="0 0 0 1" BorderBrush="#CCC" Padding="5">
                        <StackPanel>
                            <TextBlock Text="{Binding RoomName}" FontWeight="Bold" FontSize="16"/>
                            <TextBlock Text="{Binding DateText}" FontSize="12"/>
                            <TextBlock Text="{Binding TimeText}" FontSize="12"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
