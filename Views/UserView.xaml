<UserControl x:Class="EscapeRoom.Views.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <StackPanel>
            <TextBlock Text="Dane u¿ytkownika" FontSize="18" FontWeight="Bold" Margin="0,0,0,14"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                <TextBlock Text="Imiê:" Width="80"/>
                <TextBox Text="{Binding Imie, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsEditing}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                <TextBlock Text="Nazwisko:" Width="80"/>
                <TextBox Text="{Binding Nazwisko, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsEditing}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                <TextBlock Text="E-mail:" Width="80"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsEditing}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                <TextBlock Text="Telefon:" Width="80"/>
                <TextBox Text="{Binding Telefon, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsEditing}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                <TextBlock Text="Admin:" Width="80"/>
                <CheckBox IsChecked="{Binding Admin}" IsEnabled="{Binding IsEditing}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Button Content="Edytuj"
                        Command="{Binding EditCommand}"
                        IsEnabled="{Binding IsEditing, Converter={StaticResource InverseBoolConverter}}"
                        Margin="0,0,8,0"/>
                <Button Content="Zapisz"
                        Command="{Binding SaveCommand}"
                        IsEnabled="{Binding IsValid}"
                        Margin="0,0,8,0"/>
                <Button Content="Anuluj"
                        Command="{Binding CancelCommand}"
                        IsEnabled="{Binding IsEditing}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>

    