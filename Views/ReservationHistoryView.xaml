<UserControl x:Class="EscapeRoom.Views.ReservationHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:EscapeRoom.ViewModels"
             mc:Ignorable="d">

    <Grid Background="#252525">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="TWOJE REZERWACJE" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Margin="20,20,20,10" 
                   Foreground="White"
                   HorizontalAlignment="Center"/>

        <ListView Grid.Row="1" 
                  ItemsSource="{Binding UserReservations}" 
                  Margin="20"
                  Background="Transparent"
                  BorderThickness="1"
                  BorderBrush="Gray">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" 
                            BorderThickness="1" 
                            Padding="10" 
                            Margin="5"
                            CornerRadius="5">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="{Binding Pokoj.Nazwa}" 
                                     FontWeight="Bold" 
                                     FontSize="16"
                                     Foreground="White"/>

                            <TextBlock Grid.Row="1" 
                                     Text="{Binding DataRozpoczecia, StringFormat='Data: {0:dd.MM.yyyy HH:mm}'}"
                                     Margin="0,5,0,0"
                                     Foreground="White"/>

                            <TextBlock Grid.Row="2" 
                                     Text="{Binding Status}"
                                     Margin="0,5,0,0"
                                     Foreground="White"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>